def is_odd(number):
    "returns true if a number is odd, otherwise return false"
    return number % 2 != 0


def division(a_number, other_number):
    "Return the division of a_number by other_number"
    return a_number / other_number
